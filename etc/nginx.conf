server {
        listen       80;
	server_name _;
	access_log /home/box/nginx.log
        location ^~ /uploads/ {
    		root /home/box/web;
        }
        location ~* ^.+\.\w+$ {
    		root /home/box/web/public;
        }
	location / {
		return 404;
	}
    }

